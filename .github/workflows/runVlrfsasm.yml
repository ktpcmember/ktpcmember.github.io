
name: Run vlrfsasm

on: workflow_dispatch
jobs:
  convert:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Move directory
        run: |
            pushd tool\vlrfsasm
            systeminfo
            popd
        shell: cmd

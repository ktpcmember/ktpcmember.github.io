
{.; ;
  (c.convertSbS8
    ('
      "<?xml version=\"1.0\" encoding=\"UTF-8\"?><urlset xmlns=\"http://www.sitemaps.org/schemas/sitemap/0.9\">"
      (out
        (c.regularizeNewLine
          (c.convertS8Sb (sitemap.txt)
          "\r"
        )
        (c.empty)
      )
      "</urlset>"
    )
  )
}

{out A 16 B; ;
  (?
    (& (== A "\r") (!= "404.html" B))
    ('
      "<url><loc>https://www.ktpc.tokyo/"
      B
      "</loc></url>"
    )
    (c.empty)
  );
  (? (== A "\r") (c.empty) (' B A))
}
